# PostgreSQL для ДОКЕР-СЕТИ (ВНИМАНИЕ: host = db)
DB_HOST=db
DB_PORT=5432
DB_USER=lcp_user
DB_PASSWORD=lcp_password
DB_NAME=lcp_db

# SQLAlchemy (psycopg v3) — используем сервис db, не localhost
DATABASE_URL=postgresql+psycopg://lcp_user:lcp_password@db:5432/lcp_db

# FastAPI
SECRET_KEY=change-me-in-prod
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
BACKEND_CORS_ORIGINS=*

# Внешние ключи (НЕ коммить в git)
STRIPE_API_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
OPENAI_API_KEY=sk-xxx

# gRPC (если понадобятся)
GRPC_CERT_PATH=
GRPC_KEY_PATH=
GRPC_CA_PATH=

# Frontend
VITE_API_BASE_URL=http://backend:8000
