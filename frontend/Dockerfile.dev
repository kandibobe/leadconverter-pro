# Используем официальный образ Node.js
FROM node:20-alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем package.json и package-lock.json для установки зависимостей
# Это кэширует слой с зависимостями, если они не менялись
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальной код приложения
# (Этот шаг будет заменен монтированием тома в docker-compose для разработки)
COPY . .

# Открываем порт, на котором работает Vite
EXPOSE 5173

# Команда для запуска сервера разработки Vite
CMD ["npm", "run", "dev"]